ci: true
servers:
  - name: db
    boot:
        script: deploy/boot_server.sh
        logs:
        - 'full path to a log file'
        ports:
        - 9042
  - name: app
    boot:
        script: deploy/boot_server.sh
        logs:
        - 'full path to a log file'
    start:
        script: deploy/start_server.sh
        logs:
        - 'full path to a log file'
        ports:
        - 8080
        dependencies:
            CASSANDRA_IP: db