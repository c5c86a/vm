ci: true
servers:
  - name: db1
    boot:
        script: deploy/boot_db.sh
        logs:
        - 'full path to a log file'
        ports:
        - 9042
  - name: app1
    start:
        script: deploy/start_ss7cli.sh
        logs:
        - 'full path to a log file'
        ports:
        - 8080
        dependencies:
            CASSANDRA_IP: db